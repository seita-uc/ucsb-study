int cols = 5;
int rows = 5;
float[][] x = new float[cols][rows];
float[][] y = new float[cols][rows];

void setup(){
  size(500, 500);
  background(0);
  populateGrid();
}

void draw(){
  background(255);
  showGrid();
}

void populateGrid() {
    for(int i = 0; i < cols; i++) {
        float xT = map(i, 0, cols, 0, width);
    for(int i = 0; i < cols; i++) {
    }
    }
}
